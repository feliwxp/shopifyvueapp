/**
* Validation code for field amount on donation
* @param {import("gadget-server").DonationAmountFieldValidationContext } context - All the useful bits for running this validation.
*/
module.exports = async ({api, record, errors, logger, field}) => {
  // TODO: implement validation
  // access things like `record.amount` to get the fields's data
  // add to the `errors` with `errors.add('amount', "did not pass the validation")`
};